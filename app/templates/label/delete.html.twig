{% include 'default/_form.html.twig' with {
    page_title: 'title_label_delete',
    page_title_param: {'%id%': label.id},
    attr: {
        method: 'DELETE',
        action: url('label_delete', {'id': label.id}),
    },
    back_to_list_path: 'label_index',
} %}