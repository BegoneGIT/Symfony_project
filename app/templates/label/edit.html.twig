{% include 'default/_form.html.twig' with {
    page_title: 'title_label_edit',
    page_title_param: {'%id%': label.id},
    attr: {
        method: 'PUT',
        action: url('label_edit', {'code': label.code}),
    },
    back_to_list_path: 'label_index',
} %}