{% include 'default/_form.html.twig' with {
    page_title: 'title_user_delete',
    page_title_param: {'%id%': user.id},
    attr: {
        method: 'DELETE',
        action: url('admin_delete', {'id': user.id}),
    },
    back_to_list_path: 'user_show',
} %}