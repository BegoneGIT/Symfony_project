{% include 'default/_form.html.twig' with {
    page_title: 'title_wallet_delete'|trans,
    page_title_param: {'%id%': wallet.id},
    attr: {
        method: 'DELETE',
        action: url('wallet_delete', {'id': wallet.id}),
    },
    back_to_list_path: 'wallet_index',
} %}